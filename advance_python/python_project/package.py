# package
# packages are a way of structuring python's module namespace by using ''dotted module names''
# a package can have one or more modules which means, a package is collection of modules and packages
# a package can contain package
# package is nothing but a directly/ folder

# creating package
# package is nothing but a directory/ folder which MUST contain a special file called __init__.py
# __init__.py file can empty , it indicates that the directory it contains is a python package, so it can be imported the same way a module can be imported.
