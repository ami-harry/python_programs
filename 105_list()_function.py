# list function
# this is used to create a list. it returns a mutable list of elements
# syntax >--> list() --> this returns an empty list
# list(iterable_object)

# we can use list function with range too
# list(range(start:stop:stepsize))

# we can access and mofidy list values using loops

# a = list()  # this will return an empty list
# print(a)
# print(type(a))

# passing the string in list function
a = list("Harry")
print(a)
print(type(a))


# passing the range value to create a list
b = list(range(0, 5))
print(b)
print(type(b))
